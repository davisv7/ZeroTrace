{
  "name": "zerotrace",
  "version": "1.0.0",
  "description": "IPFS chat app",
  "main": "index.js",
  "scripts": {
    "start": "http-server -c-1 -p 12345 public",
    "compile": "browserify src/app.js -o public/js/app.js -d",
    "sc": "browserify src/app.js -o public/js/app.js -d && http-server -c-1 -p 12345 public"
  },
  "author": "Vincent Davis",
  "license": "MIT",
  "dependencies": {
    "browserify": "^16.2.3",
    "http-server": "^0.11.1",
    "ipfs": "^0.33.1",
    "ipfs-pubsub-room": "^1.2.1"
  }
}
